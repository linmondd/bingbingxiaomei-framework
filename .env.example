# 冰冰小美框架 — 环境变量配置
#
# 复制此文件为 .env 并填入实际值：
#   cp .env.example .env
#
# .env 已在 .gitignore 中，不会被提交到 Git。

# Proma / AI API Key（用于 LLM 提取观点）
# 可从 Proma 设置页面获取
PROMA_API_KEY=

# Proma API 地址（默认值通常不需要修改）
# PROMA_BASE_URL=https://api.promap.live/v1
